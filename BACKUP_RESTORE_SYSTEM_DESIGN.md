# منظومة النسخ الاحتياطي والاستعادة الاحترافية
# Professional Backup & Restore System Design

**تاريخ التصميم:** 2025-12-22  
**الإصدار:** 1.0  
**الحالة:** ✅ جاهز للتنفيذ

---

## 🎯 الأهداف الرئيسية

### 1. الأمان والموثوقية
- ✅ منع فقدان البيانات
- ✅ منع استعادة نسخة غير متوافقة
- ✅ حماية من الأخطاء البشرية
- ✅ تشفير البيانات الحساسة

### 2. التوافق مع معايير ERP
- ✅ مشابه لـ Zoho Books / Odoo / Next ERP
- ✅ Validation قبل الاستعادة
- ✅ تقرير تفصيلي قبل التنفيذ
- ✅ Double Confirmation

### 3. عدم التأثير على المنطق المحاسبي
- ⚠️ **Additive Only** - لا تعديل على منطق القيود
- ⚠️ **No Direct Changes** - لا تعديل على الفواتير أو الأرصدة
- ⚠️ **Read-Only Operations** - القراءة فقط من الجداول المحاسبية

---

## 📊 هيكل النسخة الاحتياطية (Backup Structure)

### صيغة الملف: JSON
```json
{
  "metadata": {
    "version": "2.0",
    "system_version": "1.0.0",
    "created_at": "2025-12-22T10:30:00Z",
    "created_by": "user_id",
    "company_id": "uuid",
    "company_name": "اسم الشركة",
    "backup_type": "full",
    "total_records": 1500,
    "checksum": "sha256_hash"
  },
  "schema_info": {
    "tables": ["companies", "chart_of_accounts", ...],
    "table_versions": {
      "invoices": "v2.1",
      "journal_entries": "v1.5"
    }
  },
  "data": {
    "companies": [...],
    "chart_of_accounts": [...],
    "customers": [...],
    "suppliers": [...],
    "products": [...],
    "invoices": [...],
    "invoice_items": [...],
    "bills": [...],
    "bill_items": [...],
    "sales_orders": [...],
    "sales_order_items": [...],
    "purchase_orders": [...],
    "purchase_order_items": [...],
    "journal_entries": [...],
    "journal_entry_lines": [...],
    "payments": [...],
    "inventory_transactions": [...],
    "sales_returns": [...],
    "sales_return_items": [...],
    "purchase_returns": [...],
    "purchase_return_items": [...],
    "customer_credits": [...],
    "vendor_credits": [...],
    "employees": [...],
    "fixed_assets": [...],
    "branches": [...],
    "cost_centers": [...],
    "warehouses": [...]
  },
  "excluded_data": {
    "reason": "security",
    "tables": ["auth.users", "company_invitations", "audit_logs"]
  }
}
```

---

## 🔐 البيانات المستثناة (Excluded Data)

### ❌ لن يتم تصديرها (أمان):
1. **كلمات المرور** - `auth.users.encrypted_password`
2. **Tokens** - `auth.sessions`, `auth.refresh_tokens`
3. **API Keys** - أي مفاتيح API
4. **Audit Logs** - سجلات التدقيق (اختياري)
5. **Company Invitations** - الدعوات المعلقة

### ✅ سيتم تصديرها:
- جميع البيانات المحاسبية والتشغيلية
- الإعدادات والتكوينات
- العلاقات بين الجداول (Foreign Keys)

---

## 📤 آلية التصدير (Export Mechanism)

### الخطوات:
1. **التحقق من الصلاحيات** - Owner/Admin فقط
2. **جمع البيانات** - حسب ترتيب العلاقات
3. **استبعاد البيانات الحساسة**
4. **إنشاء Checksum** - للتحقق من السلامة
5. **إنشاء ملف JSON**
6. **تسجيل في Audit Log**

### ترتيب الجداول (Table Order):
```javascript
const EXPORT_ORDER = [
  // 1. الجداول المستقلة (No Dependencies)
  'companies',
  'chart_of_accounts',
  'customers',
  'suppliers',
  'products',
  'employees',
  'branches',
  'cost_centers',
  'warehouses',
  
  // 2. المستندات الرئيسية
  'invoices',
  'bills',
  'sales_orders',
  'purchase_orders',
  'estimates',
  
  // 3. تفاصيل المستندات
  'invoice_items',
  'bill_items',
  'sales_order_items',
  'purchase_order_items',
  'estimate_items',
  
  // 4. المرتجعات
  'sales_returns',
  'sales_return_items',
  'purchase_returns',
  'purchase_return_items',
  
  // 5. القيود والمدفوعات
  'journal_entries',
  'journal_entry_lines',
  'payments',
  
  // 6. المخزون والأصول
  'inventory_transactions',
  'fixed_assets',
  
  // 7. الإشعارات
  'customer_credits',
  'vendor_credits'
]
```

---

## 📥 آلية الاستعادة (Restore Mechanism)

### المراحل الإلزامية:

#### 1️⃣ مرحلة التحميل (Upload Phase)
- رفع ملف JSON
- التحقق من الصيغة الأساسية
- فحص Checksum

#### 2️⃣ مرحلة التحقق (Validation Phase) ⚠️ **إلزامية**

##### أ. التحقق من التوافق:
```javascript
const validationChecks = {
  // 1. إصدار النظام
  systemVersion: {
    current: "1.0.0",
    backup: "1.0.0",
    compatible: true
  },

  // 2. هيكل الجداول
  schemaCompatibility: {
    missingTables: [],
    extraTables: [],
    columnMismatches: []
  },

  // 3. company_id
  companyId: {
    backup: "uuid-from-backup",
    target: "uuid-current",
    match: false // يجب أن يكون false للاستعادة على شركة جديدة
  },

  // 4. سلامة العلاقات (Foreign Keys)
  foreignKeyIntegrity: {
    valid: true,
    brokenReferences: []
  },

  // 5. سلامة البيانات المحاسبية
  accountingIntegrity: {
    balancedEntries: true,
    unbalancedCount: 0,
    orphanedRecords: []
  }
}
```

##### ب. تقرير ما قبل الاستعادة:
```javascript
const restoreReport = {
  summary: {
    totalRecords: 1500,
    recordsToInsert: 1500,
    recordsToUpdate: 0,
    recordsToDelete: 0,
    estimatedTime: "2-3 minutes"
  },

  breakdown: {
    companies: { count: 1, action: "replace" },
    chart_of_accounts: { count: 50, action: "insert" },
    customers: { count: 100, action: "insert" },
    invoices: { count: 200, action: "insert" },
    journal_entries: { count: 400, action: "insert" }
  },

  warnings: [
    "⚠️ سيتم استبدال بيانات الشركة الحالية",
    "⚠️ لا يمكن التراجع عن هذه العملية"
  ],

  risks: {
    dataLoss: "high",
    recommendation: "backup_current_data_first"
  }
}
```

#### 3️⃣ مرحلة التأكيد المزدوج (Double Confirmation)

```javascript
// التأكيد الأول
const confirmation1 = {
  message: "هل أنت متأكد من استعادة هذه النسخة؟",
  details: restoreReport,
  requiresTyping: "RESTORE" // يجب كتابة كلمة RESTORE
}

// التأكيد الثاني (بعد 3 ثواني)
const confirmation2 = {
  message: "تحذير نهائي: سيتم استبدال جميع البيانات الحالية",
  requiresCheckbox: true,
  checkboxText: "أفهم أن هذه العملية لا يمكن التراجع عنها"
}
```

#### 4️⃣ مرحلة التنفيذ (Execution Phase)

##### أ. نمط الاستعادة:
```javascript
const RESTORE_MODES = {
  // النمط الوحيد المدعوم حالياً
  RESTORE_TO_EMPTY: {
    description: "استعادة على شركة فارغة أو جديدة",
    requirements: [
      "الشركة المستهدفة يجب أن تكون فارغة",
      "لا توجد فواتير أو قيود محاسبية"
    ],
    validation: async (companyId) => {
      const { count: invoiceCount } = await supabase
        .from('invoices')
        .select('*', { count: 'exact', head: true })
        .eq('company_id', companyId)

      const { count: journalCount } = await supabase
        .from('journal_entries')
        .select('*', { count: 'exact', head: true })
        .eq('company_id', companyId)

      return invoiceCount === 0 && journalCount === 0
    }
  },

  // نمط متقدم (معطل حالياً)
  RESTORE_WITH_MERGE: {
    description: "دمج ذكي مع البيانات الحالية",
    status: "disabled",
    reason: "معقد جداً - يحتاج تطوير إضافي"
  }
}
```

##### ب. خطوات التنفيذ:
1. **بدء Transaction** - لضمان Atomicity
2. **حذف البيانات الحالية** (إذا لزم الأمر)
3. **استعادة حسب الترتيب** - نفس ترتيب التصدير
4. **التحقق من النتائج**
5. **Commit أو Rollback**
6. **تسجيل في Audit Log**

---

## 🔒 الأمان والصلاحيات

### الصلاحيات المطلوبة:
```typescript
interface BackupPermissions {
  export: ['owner', 'admin']
  restore: ['owner'] // Owner فقط
  view_history: ['owner', 'admin']
}
```

### Audit Log:
```typescript
interface BackupAuditLog {
  id: string
  company_id: string
  user_id: string
  action: 'export' | 'restore' | 'restore_failed'
  timestamp: string
  details: {
    backup_file: string
    records_count: number
    duration_seconds: number
    success: boolean
    error_message?: string
  }
}
```

---

## 🎨 تجربة المستخدم (UX)

### 1. صفحة النسخ الاحتياطي

```
┌─────────────────────────────────────────────┐
│  📦 النسخ الاحتياطي والاستعادة              │
├─────────────────────────────────────────────┤
│                                             │
│  📤 تصدير نسخة احتياطية                    │
│  ┌─────────────────────────────────────┐   │
│  │ ⚠️ تحذير:                           │   │
│  │ النسخة تحتوي على بيانات حساسة      │   │
│  │ يُرجى حفظها في مكان آمن            │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  [🔽 تصدير نسخة احتياطية كاملة]            │
│                                             │
│  آخر نسخة احتياطية: 2025-12-20 10:30 AM   │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│  📥 استعادة نسخة احتياطية                  │
│  ┌─────────────────────────────────────┐   │
│  │ ⚠️ تحذير خطير:                      │   │
│  │ • سيتم استبدال جميع البيانات الحالية│   │
│  │ • لا يمكن التراجع عن هذه العملية    │   │
│  │ • يُنصح بعمل نسخة احتياطية أولاً    │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  [📁 اختيار ملف النسخة الاحتياطية]         │
│                                             │
└─────────────────────────────────────────────┘
```

### 2. نافذة التحقق والتأكيد

```
┌─────────────────────────────────────────────┐
│  🔍 فحص النسخة الاحتياطية                  │
├─────────────────────────────────────────────┤
│                                             │
│  ✅ التحقق من التوافق                      │
│  ✅ فحص سلامة البيانات                     │
│  ✅ التحقق من العلاقات                     │
│                                             │
│  📊 ملخص النسخة:                           │
│  • إجمالي السجلات: 1,500                   │
│  • الفواتير: 200                           │
│  • القيود المحاسبية: 400                   │
│  • العملاء: 100                            │
│                                             │
│  ⚠️ تحذيرات:                               │
│  • سيتم استبدال بيانات الشركة الحالية     │
│  • الوقت المتوقع: 2-3 دقائق                │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ للمتابعة، اكتب: RESTORE             │   │
│  │ [________________]                   │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  [إلغاء]  [متابعة →]                       │
│                                             │
└─────────────────────────────────────────────┘
```

### 3. شريط التقدم

```
┌─────────────────────────────────────────────┐
│  ⏳ جاري استعادة النسخة الاحتياطية...      │
├─────────────────────────────────────────────┤
│                                             │
│  [████████████████░░░░░░░░] 65%            │
│                                             │
│  الخطوة الحالية:                           │
│  📝 استعادة القيود المحاسبية (320/400)     │
│                                             │
│  ⚠️ يُرجى عدم إغلاق النافذة               │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 📁 الملفات المطلوب إنشاؤها/تعديلها

### 1. Backend API Routes

#### `app/api/backup/export/route.ts`
- تصدير النسخة الاحتياطية
- التحقق من الصلاحيات
- جمع البيانات
- إنشاء JSON

#### `app/api/backup/validate/route.ts`
- فحص النسخة الاحتياطية
- التحقق من التوافق
- إنشاء تقرير التحقق

#### `app/api/backup/restore/route.ts`
- استعادة النسخة الاحتياطية
- التنفيذ الفعلي
- Rollback في حالة الفشل

#### `app/api/backup/history/route.ts`
- عرض سجل النسخ الاحتياطية
- من Audit Log

### 2. Frontend Components

#### `app/settings/backup/page.tsx`
- الصفحة الرئيسية للنسخ الاحتياطي
- واجهة التصدير والاستعادة

#### `components/backup/ExportDialog.tsx`
- نافذة التصدير
- Progress Indicator

#### `components/backup/RestoreDialog.tsx`
- نافذة الاستعادة
- Validation Report
- Double Confirmation

#### `components/backup/ValidationReport.tsx`
- عرض تقرير التحقق
- التحذيرات والمخاطر

### 3. Utility Functions

#### `lib/backup/export-utils.ts`
- دوال التصدير
- ترتيب الجداول
- استبعاد البيانات الحساسة

#### `lib/backup/restore-utils.ts`
- دوال الاستعادة
- التحقق من التوافق
- معالجة الأخطاء

#### `lib/backup/validation-utils.ts`
- دوال التحقق
- فحص السلامة
- إنشاء التقارير

---

## ✅ معايير النجاح

### 1. الوظائف الأساسية
- ✅ تصدير نسخة احتياطية كاملة
- ✅ استعادة نسخة احتياطية بأمان
- ✅ التحقق من التوافق قبل الاستعادة
- ✅ تقرير تفصيلي قبل التنفيذ

### 2. الأمان
- ✅ صلاحيات Owner/Admin فقط
- ✅ استبعاد البيانات الحساسة
- ✅ Audit Logging لجميع العمليات
- ✅ Double Confirmation

### 3. تجربة المستخدم
- ✅ واجهة واضحة وبسيطة
- ✅ رسائل مفهومة (غير تقنية)
- ✅ Progress Indicators
- ✅ معالجة الأخطاء بشكل احترافي

### 4. الموثوقية
- ✅ اختبار: تصدير → حذف → استعادة → تطابق 100%
- ✅ لا فقدان بيانات
- ✅ لا تكرار سجلات
- ✅ الأرصدة متطابقة

---

## 🚀 خطة التنفيذ

### المرحلة 1: Backend (API Routes)
1. إنشاء `/api/backup/export/route.ts`
2. إنشاء `/api/backup/validate/route.ts`
3. إنشاء `/api/backup/restore/route.ts`
4. إنشاء Utility Functions

### المرحلة 2: Frontend (UI)
1. إنشاء صفحة `/settings/backup`
2. إنشاء مكونات الحوارات
3. إنشاء Progress Indicators
4. إضافة التحذيرات والتأكيدات

### المرحلة 3: Testing
1. اختبار التصدير
2. اختبار التحقق
3. اختبار الاستعادة
4. اختبار السيناريو الكامل

### المرحلة 4: Documentation
1. دليل المستخدم
2. دليل المطور
3. FAQ

---

## ⚠️ القيود والمحددات

### ما لن يتم تنفيذه (حالياً):
1. **Restore with Merge** - دمج ذكي (معقد جداً)
2. **Incremental Backup** - نسخ احتياطية تزايدية
3. **Scheduled Backups** - نسخ احتياطية مجدولة
4. **Cloud Storage Integration** - تخزين سحابي تلقائي
5. **Encryption** - تشفير الملفات (يمكن إضافته لاحقاً)

### ما سيتم تنفيذه:
1. ✅ **Full Backup** - نسخة احتياطية كاملة
2. ✅ **Restore to Empty Company** - استعادة على شركة فارغة
3. ✅ **Validation Phase** - مرحلة التحقق
4. ✅ **Audit Logging** - تسجيل العمليات
5. ✅ **Owner/Admin Only** - صلاحيات محدودة

---

## 📝 ملاحظات مهمة

### 1. عدم التأثير على المنطق المحاسبي
- ⚠️ **لا تعديل على منطق القيود المحاسبية**
- ⚠️ **لا تعديل على حسابات الفواتير**
- ⚠️ **لا تعديل على العلاقات بين الجداول**
- ✅ **فقط قراءة واستعادة البيانات كما هي**

### 2. التوافق مع النظام الحالي
- ✅ يعمل مع الكود الموجود في `app/settings/page.tsx`
- ✅ يستخدم نفس نظام الصلاحيات
- ✅ يستخدم نفس Audit Log
- ✅ لا يتطلب تغييرات في قاعدة البيانات

### 3. الأداء
- ⏱️ التصدير: 1-2 دقيقة لـ 10,000 سجل
- ⏱️ الاستعادة: 2-3 دقائق لـ 10,000 سجل
- 💾 حجم الملف: ~1-5 MB لشركة متوسطة

---

## ✅ جاهز للتنفيذ

هذا التصميم:
- ✅ آمن وموثوق
- ✅ لا يؤثر على المنطق المحاسبي
- ✅ متوافق مع معايير ERP
- ✅ قابل للاختبار
- ✅ قابل للصيانة

**الخطوة التالية:** بدء التنفيذ الفعلي للكود

