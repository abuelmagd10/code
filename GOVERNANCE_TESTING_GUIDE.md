# 🧪 دليل اختبار نظام الحوكمة الشامل

## 📋 نظرة عامة

هذا الدليل يوضح كيفية اختبار نظام الحوكمة المطبق في ERB VitaSlims للتأكد من أن كل دور يرى البيانات المناسبة له فقط.

## 🎯 أهداف الاختبار

1. **التأكد من تطبيق الحوكمة الصحيحة** - كل دور يرى البيانات المخصصة له فقط
2. **منع تسريب البيانات** - لا يمكن لأي دور رؤية بيانات خارج صلاحياته
3. **التحقق من الأداء** - النظام يعمل بكفاءة مع الفلاتر المطبقة

## 🔐 الأدوار المطلوب اختبارها

### 1️⃣ الموظف (Staff)
- **الصلاحيات**: يرى فقط أوامر البيع التي أنشأها بنفسه
- **الفلاتر المطبقة**: 
  - `company_id = user_company`
  - `branch_id = user_branch`
  - `cost_center_id = user_cost_center`
  - `warehouse_id = user_warehouse`
  - `created_by_user_id = user_id`

### 2️⃣ المحاسب (Accountant)
- **الصلاحيات**: يرى جميع أوامر البيع في فرعه مع إمكانية فلترة حسب الموظف
- **الفلاتر المطبقة**:
  - `company_id = user_company`
  - `branch_id = user_branch`
  - فلتر اختياري: `created_by_user_id = selected_employee`

### 3️⃣ مدير الفرع (Manager)
- **الصلاحيات**: يرى جميع أوامر البيع في فرعه
- **الفلاتر المطبقة**:
  - `company_id = user_company`
  - `branch_id = user_branch`

### 4️⃣ المدير العام/المالك (Owner/Admin)
- **الصلاحيات**: يرى جميع أوامر البيع في الشركة
- **الفلاتر المطبقة**:
  - `company_id = user_company`

## 🧪 سيناريوهات الاختبار

### السيناريو 1: إعداد بيانات الاختبار

#### الخطوة 1: إنشاء هيكل الشركة
```sql
-- إنشاء شركة اختبار
INSERT INTO companies (id, name, created_at) 
VALUES ('test-company-001', 'شركة الاختبار', NOW());

-- إنشاء فرعين
INSERT INTO branches (id, company_id, name, is_main, created_at) VALUES
('branch-main', 'test-company-001', 'الفرع الرئيسي', true, NOW()),
('branch-secondary', 'test-company-001', 'الفرع الثانوي', false, NOW());

-- إنشاء مراكز تكلفة
INSERT INTO cost_centers (id, company_id, branch_id, name, created_at) VALUES
('cc-main-001', 'test-company-001', 'branch-main', 'مركز التكلفة الرئيسي', NOW()),
('cc-secondary-001', 'test-company-001', 'branch-secondary', 'مركز التكلفة الثانوي', NOW());

-- إنشاء مخازن
INSERT INTO warehouses (id, company_id, branch_id, name, is_main, created_at) VALUES
('wh-main-001', 'test-company-001', 'branch-main', 'المخزن الرئيسي', true, NOW()),
('wh-secondary-001', 'test-company-001', 'branch-secondary', 'المخزن الثانوي', true, NOW());
```

#### الخطوة 2: إنشاء المستخدمين
```sql
-- إنشاء المستخدمين (يتم عبر Supabase Auth)
-- user-staff-001: موظف في الفرع الرئيسي
-- user-accountant-001: محاسب في الفرع الرئيسي  
-- user-manager-001: مدير الفرع الرئيسي
-- user-owner-001: مالك الشركة

-- ربط المستخدمين بالشركة
INSERT INTO company_members (company_id, user_id, role, branch_id, cost_center_id, warehouse_id) VALUES
('test-company-001', 'user-staff-001', 'staff', 'branch-main', 'cc-main-001', 'wh-main-001'),
('test-company-001', 'user-accountant-001', 'accountant', 'branch-main', 'cc-main-001', 'wh-main-001'),
('test-company-001', 'user-manager-001', 'manager', 'branch-main', 'cc-main-001', 'wh-main-001'),
('test-company-001', 'user-owner-001', 'owner', NULL, NULL, NULL);
```

#### الخطوة 3: إنشاء عملاء وأوامر بيع للاختبار
```sql
-- إنشاء عملاء
INSERT INTO customers (id, company_id, branch_id, cost_center_id, name, created_by_user_id) VALUES
('customer-001', 'test-company-001', 'branch-main', 'cc-main-001', 'عميل الاختبار 1', 'user-staff-001'),
('customer-002', 'test-company-001', 'branch-main', 'cc-main-001', 'عميل الاختبار 2', 'user-accountant-001'),
('customer-003', 'test-company-001', 'branch-secondary', 'cc-secondary-001', 'عميل الاختبار 3', 'user-manager-001');

-- إنشاء أوامر بيع
INSERT INTO sales_orders (id, company_id, branch_id, cost_center_id, warehouse_id, customer_id, so_number, so_date, total_amount, status, created_by_user_id) VALUES
-- أوامر الموظف في الفرع الرئيسي
('so-staff-001', 'test-company-001', 'branch-main', 'cc-main-001', 'wh-main-001', 'customer-001', 'SO-STAFF-001', '2024-01-15', 1000.00, 'draft', 'user-staff-001'),
('so-staff-002', 'test-company-001', 'branch-main', 'cc-main-001', 'wh-main-001', 'customer-001', 'SO-STAFF-002', '2024-01-16', 1500.00, 'sent', 'user-staff-001'),

-- أوامر المحاسب في الفرع الرئيسي
('so-accountant-001', 'test-company-001', 'branch-main', 'cc-main-001', 'wh-main-001', 'customer-002', 'SO-ACC-001', '2024-01-17', 2000.00, 'draft', 'user-accountant-001'),

-- أوامر المدير في الفرع الثانوي
('so-manager-001', 'test-company-001', 'branch-secondary', 'cc-secondary-001', 'wh-secondary-001', 'customer-003', 'SO-MGR-001', '2024-01-18', 2500.00, 'sent', 'user-manager-001');
```

### السيناريو 2: اختبار دور الموظف (Staff)

#### الاختبار المتوقع:
- يجب أن يرى الموظف أمرين فقط: `SO-STAFF-001` و `SO-STAFF-002`
- لا يجب أن يرى أوامر المحاسب أو المدير

#### خطوات الاختبار:
1. **تسجيل الدخول** كموظف (`user-staff-001`)
2. **الانتقال** إلى صفحة أوامر البيع
3. **التحقق** من عدد الأوامر المعروضة = 2
4. **التحقق** من أرقام الأوامر = `SO-STAFF-001`, `SO-STAFF-002`

#### الاستعلام المتوقع:
```sql
SELECT * FROM sales_orders 
WHERE company_id = 'test-company-001'
  AND branch_id = 'branch-main'
  AND cost_center_id = 'cc-main-001'
  AND warehouse_id = 'wh-main-001'
  AND created_by_user_id = 'user-staff-001';
```

#### النتيجة المتوقعة:
```
✅ عدد الأوامر: 2
✅ الأوامر: SO-STAFF-001, SO-STAFF-002
❌ لا يرى: SO-ACC-001, SO-MGR-001
```

### السيناريو 3: اختبار دور المحاسب (Accountant)

#### الاختبار المتوقع:
- يجب أن يرى المحاسب 3 أوامر: `SO-STAFF-001`, `SO-STAFF-002`, `SO-ACC-001`
- لا يجب أن يرى أوامر الفرع الثانوي: `SO-MGR-001`
- يجب أن يتمكن من فلترة حسب الموظف

#### خطوات الاختبار:
1. **تسجيل الدخول** كمحاسب (`user-accountant-001`)
2. **الانتقال** إلى صفحة أوامر البيع
3. **التحقق** من عدد الأوامر المعروضة = 3
4. **اختبار فلتر الموظف** - اختيار الموظف (`user-staff-001`)
5. **التحقق** من عدد الأوامر بعد الفلترة = 2

#### الاستعلام المتوقع (بدون فلترة):
```sql
SELECT * FROM sales_orders 
WHERE company_id = 'test-company-001'
  AND branch_id = 'branch-main';
```

#### الاستعلام المتوقع (مع فلترة الموظف):
```sql
SELECT * FROM sales_orders 
WHERE company_id = 'test-company-001'
  AND branch_id = 'branch-main'
  AND created_by_user_id = 'user-staff-001';
```

#### النتيجة المتوقعة:
```
✅ عدد الأوامر (بدون فلترة): 3
✅ الأوامر: SO-STAFF-001, SO-STAFF-002, SO-ACC-001
✅ عدد الأوامر (مع فلترة الموظف): 2
✅ الأوامر المفلترة: SO-STAFF-001, SO-STAFF-002
❌ لا يرى: SO-MGR-001
```

### السيناريو 4: اختبار دور مدير الفرع (Manager)

#### الاختبار المتوقع:
- يجب أن يرى المدير جميع أوامر فرعه: `SO-STAFF-001`, `SO-STAFF-002`, `SO-ACC-001`
- لا يجب أن يرى أوامر الفروع الأخرى: `SO-MGR-001`

#### خطوات الاختبار:
1. **تسجيل الدخول** كمدير فرع (`user-manager-001`)
2. **الانتقال** إلى صفحة أوامر البيع
3. **التحقق** من عدد الأوامر المعروضة = 3
4. **التحقق** من أرقام الأوامر

#### النتيجة المتوقعة:
```
✅ عدد الأوامر: 3
✅ الأوامر: SO-STAFF-001, SO-STAFF-002, SO-ACC-001
❌ لا يرى: SO-MGR-001 (من فرع آخر)
```

### السيناريو 5: اختبار دور المالك/المدير العام (Owner/Admin)

#### الاختبار المتوقع:
- يجب أن يرى المالك جميع الأوامر في الشركة: 4 أوامر
- يجب أن يتمكن من فلترة حسب الفرع والموظف

#### خطوات الاختبار:
1. **تسجيل الدخول** كمالك (`user-owner-001`)
2. **الانتقال** إلى صفحة أوامر البيع
3. **التحقق** من عدد الأوامر المعروضة = 4
4. **اختبار فلتر الفرع** - اختيار الفرع الرئيسي
5. **التحقق** من عدد الأوامر بعد الفلترة = 3

#### النتيجة المتوقعة:
```
✅ عدد الأوامر (بدون فلترة): 4
✅ الأوامر: SO-STAFF-001, SO-STAFF-002, SO-ACC-001, SO-MGR-001
✅ عدد الأوامر (فلترة الفرع الرئيسي): 3
✅ الأوامر المفلترة: SO-STAFF-001, SO-STAFF-002, SO-ACC-001
```

## 🔍 اختبارات الأمان

### اختبار 1: محاولة الوصول المباشر
```javascript
// محاولة الوصول لأمر خارج الصلاحيات عبر API
fetch('/api/sales-orders/so-mgr-001', {
  headers: { 'Authorization': 'Bearer staff-user-token' }
})
// النتيجة المتوقعة: 403 Forbidden
```

### اختبار 2: محاولة تعديل البيانات
```javascript
// محاولة تعديل أمر لا ينتمي للمستخدم
fetch('/api/sales-orders/so-acc-001', {
  method: 'PUT',
  headers: { 'Authorization': 'Bearer staff-user-token' },
  body: JSON.stringify({ total_amount: 9999 })
})
// النتيجة المتوقعة: 403 Forbidden
```

### اختبار 3: محاولة إنشاء أمر في فرع آخر
```javascript
// محاولة إنشاء أمر في فرع غير مصرح به
fetch('/api/sales-orders', {
  method: 'POST',
  headers: { 'Authorization': 'Bearer staff-user-token' },
  body: JSON.stringify({
    branch_id: 'branch-secondary', // فرع غير مصرح به
    customer_id: 'customer-003',
    total_amount: 1000
  })
})
// النتيجة المتوقعة: 403 Forbidden
```

## 📊 تقرير نتائج الاختبار

### نموذج تقرير الاختبار

```
🧪 تقرير اختبار نظام الحوكمة
=====================================
تاريخ الاختبار: [DATE]
المختبر: [TESTER_NAME]

📋 نتائج الاختبار:

1️⃣ دور الموظف (Staff):
   ✅ يرى أوامره فقط (2/2)
   ✅ لا يرى أوامر الآخرين (0/2)
   ✅ لا يستطيع الوصول لأوامر خارج صلاحياته
   
2️⃣ دور المحاسب (Accountant):
   ✅ يرى أوامر الفرع (3/3)
   ✅ فلترة الموظف تعمل (2/3 عند الفلترة)
   ✅ لا يرى أوامر الفروع الأخرى (0/1)
   
3️⃣ دور مدير الفرع (Manager):
   ✅ يرى جميع أوامر الفرع (3/3)
   ✅ لا يرى أوامر الفروع الأخرى (0/1)
   
4️⃣ دور المالك (Owner):
   ✅ يرى جميع الأوامر (4/4)
   ✅ فلترة الفرع تعمل (3/4 عند الفلترة)

🔒 اختبارات الأمان:
   ✅ منع الوصول المباشر
   ✅ منع التعديل غير المصرح
   ✅ منع الإنشاء في فروع غير مصرحة

📊 النتيجة الإجمالية: ✅ نجح (100%)
```

## 🚨 مؤشرات الخطر

### 🔴 خطر عالي - يجب الإصلاح فوراً
- موظف يرى أوامر موظفين آخرين
- محاسب يرى أوامر فروع أخرى
- إمكانية الوصول المباشر لبيانات غير مصرح بها

### 🟡 خطر متوسط - يحتاج مراجعة
- بطء في تحميل البيانات (أكثر من 3 ثوان)
- عدم عمل الفلاتر بشكل صحيح
- رسائل خطأ غير واضحة

### 🟢 آمن
- كل دور يرى البيانات المناسبة له فقط
- الفلاتر تعمل بشكل صحيح
- الأداء مقبول (أقل من 2 ثانية)

## 📋 قائمة مراجعة الاختبار

### قبل البدء
- [ ] تم تطبيق إصلاحات قاعدة البيانات
- [ ] تم تحديث نظام الحوكمة في الكود
- [ ] تم إنشاء بيانات الاختبار
- [ ] تم إنشاء المستخدمين للاختبار

### أثناء الاختبار
- [ ] اختبار دور Staff
- [ ] اختبار دور Accountant
- [ ] اختبار دور Manager  
- [ ] اختبار دور Owner/Admin
- [ ] اختبار الفلاتر
- [ ] اختبار الأمان

### بعد الاختبار
- [ ] توثيق النتائج
- [ ] إصلاح أي مشاكل
- [ ] إعادة الاختبار
- [ ] الموافقة على النشر

## 🎯 معايير النجاح

### ✅ النظام ناجح إذا:
1. كل دور يرى البيانات المناسبة له فقط
2. لا يمكن لأي دور الوصول لبيانات خارج صلاحياته
3. الفلاتر تعمل بشكل صحيح
4. الأداء مقبول (أقل من 3 ثوان)
5. رسائل الخطأ واضحة ومفيدة

### ❌ النظام يحتاج إصلاح إذا:
1. أي دور يرى بيانات خارج صلاحياته
2. إمكانية الوصول المباشر لبيانات غير مصرح بها
3. الفلاتر لا تعمل أو تعطي نتائج خاطئة
4. الأداء بطيء جداً (أكثر من 5 ثوان)
5. أخطاء في النظام أو رسائل غير واضحة

---

**تاريخ إنشاء الدليل**: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')  
**الإصدار**: 1.0  
**المطور**: ERB VitaSlims Team