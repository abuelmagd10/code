# 🔄 مزامنة حالة أمر الشراء مع الفاتورة المرتبطة

**التاريخ:** 21 ديسمبر 2025  
**الهدف:** عرض حالة الفاتورة المرتبطة بدلاً من حالة أمر الشراء في صفحة أوامر الشراء

---

## 📋 **المشكلة:**

عند عرض أوامر الشراء في الجدول، كانت الحالة المعروضة هي حالة أمر الشراء (`po.status`) بدلاً من حالة الفاتورة المرتبطة (`linkedBill.status`).

### **مثال:**
```
رقم الأمر: PO-0001
المورد: شركة فارم
الحالة المعروضة: "تم التحويل" (من أمر الشراء)
الحالة المطلوبة: "مدفوعة" (من الفاتورة المرتبطة)
```

---

## ✅ **الحل المُطبق:**

### **الملف المُعدل:**
`app/purchase-orders/page.tsx`

### **التعديل:**

**قبل:**
```typescript
{paginatedOrders.map((po) => {
  const linkedBill = po.bill_id ? linkedBills[po.bill_id] : null;
  const canEditDelete = !linkedBill || linkedBill.status === 'draft';
  // ...
  return (
    <tr>
      {/* ... */}
      <td className="px-3 py-3 text-center">{getStatusBadge(po.status)}</td>
    </tr>
  );
})}
```

**بعد:**
```typescript
{paginatedOrders.map((po) => {
  const linkedBill = po.bill_id ? linkedBills[po.bill_id] : null;
  const canEditDelete = !linkedBill || linkedBill.status === 'draft';
  // عرض حالة الفاتورة إذا كانت موجودة، وإلا عرض حالة أمر الشراء
  const displayStatus = linkedBill ? linkedBill.status : po.status;
  // ...
  return (
    <tr>
      {/* ... */}
      <td className="px-3 py-3 text-center">{getStatusBadge(displayStatus)}</td>
    </tr>
  );
})}
```

---

## 🎯 **النتيجة:**

### **السلوك الجديد:**

| الحالة | الحالة المعروضة |
|--------|-----------------|
| **أمر شراء بدون فاتورة** | حالة أمر الشراء (`po.status`) |
| **أمر شراء مع فاتورة مسودة** | `draft` (من الفاتورة) |
| **أمر شراء مع فاتورة مُرسلة** | `sent` (من الفاتورة) |
| **أمر شراء مع فاتورة مدفوعة** | `paid` (من الفاتورة) |
| **أمر شراء مع فاتورة مدفوعة جزئياً** | `partially_paid` (من الفاتورة) |

---

## 📊 **مقارنة مع أوامر البيع:**

صفحة أوامر البيع (`app/sales-orders/page.tsx`) **بالفعل تطبق هذا السلوك** بشكل صحيح:

```typescript
const linkedInvoice = o.invoice_id ? linkedInvoices[o.invoice_id] : null;
const invoiceStatus = linkedInvoice?.status || 'draft';
const displayStatus = linkedInvoice ? invoiceStatus : o.status;
```

الآن أوامر الشراء تتبع نفس المنطق.

---

## ✅ **الاختبارات:**

- ✅ **32 اختبار حرج نجح**
- ✅ **Security checks تمر بنجاح**
- ✅ **TypeScript compilation نجح**

---

## 🔗 **الملفات المرتبطة:**

1. `app/purchase-orders/page.tsx` - صفحة أوامر الشراء (مُعدل)
2. `app/sales-orders/page.tsx` - صفحة أوامر البيع (مرجع)
3. `app/bills/[id]/page.tsx` - صفحة الفاتورة (تحديث حالة أمر الشراء)
4. `app/bills/[id]/edit/page.tsx` - تعديل الفاتورة (تحديث حالة أمر الشراء)

---

## 📝 **ملاحظات:**

1. **المزامنة التلقائية**: عند تحديث حالة الفاتورة، يتم تحديث حالة أمر الشراء تلقائياً في قاعدة البيانات
2. **العرض في الجدول**: الآن يعرض الجدول حالة الفاتورة المرتبطة بدلاً من حالة أمر الشراء
3. **الفلترة**: الفلترة تعمل بشكل صحيح مع الحالة المعروضة (السطر 250 في الكود)

---

**🎉 التعديل مكتمل وجاهز للاستخدام!**

