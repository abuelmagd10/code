# ⚡ تفعيل Realtime في Supabase - دليل سريع

## 🎯 الطريقة الصحيحة (الأسهل)

### الخطوات (3 دقائق):

1. **افتح Supabase Dashboard**
   - اذهب إلى: https://app.supabase.com
   - اختر مشروعك

2. **افتح SQL Editor**
   - من القائمة الجانبية: اضغط على **"SQL Editor"**
   - اضغط **"New query"** أو **"+"**

3. **انسخ والصق السكريبت**
   - افتح ملف: `scripts/enable_realtime_tables.sql`
   - انسخ كل المحتوى (Ctrl+A ثم Ctrl+C)
   - الصقه في SQL Editor (Ctrl+V)

4. **شغّل السكريبت**
   - اضغط **"Run"** أو `Ctrl+Enter`
   - انتظر رسائل النجاح ✅

5. **انتهى!** ✅

---

## 📋 ما الذي يفعله السكريبت؟

السكريبت يفعل Realtime على الجداول التالية:

- ✅ `notifications` - الإشعارات
- ✅ `inventory_write_offs` - الإهلاك
- ✅ `inventory_transactions` - حركات المخزون
- ✅ `purchase_orders` - أوامر الشراء
- ✅ `sales_orders` - أوامر البيع
- ✅ `invoices` - الفواتير
- ✅ `approval_workflows` - الموافقات

---

## ✅ التحقق من التفعيل

بعد تشغيل السكريبت، شغّل هذا الاستعلام للتحقق:

```sql
SELECT tablename 
FROM pg_publication_tables 
WHERE pubname = 'supabase_realtime' 
  AND tablename IN (
    'notifications',
    'inventory_write_offs',
    'inventory_transactions',
    'purchase_orders',
    'sales_orders',
    'invoices',
    'approval_workflows'
  );
```

يجب أن ترى قائمة بالجداول المفعلة.

---

## ⚠️ ملاحظة مهمة

**لا تستخدم صفحة "Replication"** - هذه الصفحة لنسخ البيانات الخارجي وليست لتفعيل Realtime.

استخدم **SQL Editor** مباشرة - هذه هي الطريقة الصحيحة والأسهل.

---

## 🚀 بعد التفعيل

بمجرد تفعيل Realtime، النظام سيعمل تلقائياً:

- ✅ التحديثات ستظهر فوراً في جميع الشاشات
- ✅ لا حاجة لأي إعدادات إضافية
- ✅ جاهز للاستخدام مباشرة

---

**الوقت المتوقع: 2-3 دقائق فقط!** ⚡
