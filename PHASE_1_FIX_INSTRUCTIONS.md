# تعليمات إصلاح القيد غير المتوازن - Phase 1
# Fix Unbalanced Entry Instructions

**⚠️ مهم جداً:** ملفات `.md` هي للتوثيق فقط - **لا تُنفذ في SQL Editor!**

استخدم فقط ملفات `.sql` في Supabase SQL Editor.

---

## 📋 الخطوات المطلوبة

### الخطوة 1: عرض القيود غير المتوازنة

**الملف:** `scripts/fix_unbalanced_entries_clean.sql` (الجزء 1)

1. افتح Supabase SQL Editor
2. انسخ الجزء 1 من الملف (من `-- 1. عرض جميع القيود` حتى نهاية SELECT)
3. الصق في SQL Editor
4. اضغط Run

**النتيجة:** ستظهر جميع القيود غير المتوازنة

---

### الخطوة 2: البحث عن حساب مناسب

**الملف:** `scripts/fix_unbalanced_entries_clean.sql` (الجزء 2)

1. انسخ الجزء 2 من الملف (البحث عن حساب)
2. الصق في SQL Editor
3. اضغط Run

**النتيجة:** ستظهر قائمة بالحسابات المناسبة (مصروف، تكلفة، إلخ)

**اختر حساب مناسب** واحفظ معرفه (ID)

---

### الخطوة 3: إصلاح القيد

**الملف:** `scripts/fix_unbalanced_entries_clean.sql` (الجزء 3)

1. انسخ الجزء 3 من الملف (INSERT statement)
2. **استبدل `'YOUR_ACCOUNT_ID'`** بمعرف الحساب الذي اخترته
3. **أزل التعليق** `/* */` حول INSERT
4. الصق في SQL Editor
5. اضغط Run

**النتيجة:** يجب أن ترى "INSERT 0 1" أو رسالة نجاح

---

### الخطوة 4: التحقق من الإصلاح

**الملف:** `scripts/fix_unbalanced_entries_clean.sql` (الجزء 4)

1. انسخ الجزء 4 من الملف
2. الصق في SQL Editor
3. اضغط Run

**النتيجة المتوقعة:**
- difference = 0.00 أو أقل من 0.01
- status = '✓ متوازن'

---

### الخطوة 5: التحقق النهائي

**الملف:** `scripts/fix_unbalanced_entries_clean.sql` (الجزء 5)

1. انسخ الجزء 5 من الملف
2. الصق في SQL Editor
3. اضغط Run

**النتيجة المتوقعة:**
- unbalanced_entries_count = 0

---

## ✅ بعد الإصلاح

1. **التحقق من Phase 1:**
   - نفذ `scripts/verify_phase1_installation.sql`

2. **تنفيذ الاختبارات:**
   - استخدم `scripts/phase1_test_queries.sql`
   - أو نفذ الاختبارات يدوياً

---

## 🚨 تحذيرات

1. **لا تُنفذ ملفات .md في SQL Editor**
2. **راجع الحساب قبل الإصلاح** - تأكد أنه مناسب
3. **احتفظ بنسخة احتياطية** قبل أي تعديل

---

## 📝 الخلاصة

**الملف المطلوب:** `scripts/fix_unbalanced_entries_clean.sql`

**الخطوات:**
1. عرض القيود غير المتوازنة
2. البحث عن حساب مناسب
3. إصلاح القيد (بعد استبدال YOUR_ACCOUNT_ID)
4. التحقق من الإصلاح
5. التحقق النهائي

---

**✅ جاهز للإصلاح!**

