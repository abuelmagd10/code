# 🧹 تنظيف شامل لمخزون المستودعات - شركة "تست"
# Complete Warehouse Inventory Cleanup for Test Company "تست"

**تاريخ:** 2026-01-05  
**الهدف:** حذف جميع بيانات المخزون على مستوى المستودعات والفروع ومراكز التكلفة

---

## ⚠️ المشكلة

المخازن التابعة لشركة "تست" لا تزال تحتوي على:
- ✅ مخزون في بعض المستودعات
- ✅ مخزون سالب في بعض المستودعات
- ✅ بيانات مخزون على مستوى الفروع ومراكز التكلفة

---

## 📋 ما سيتم حذفه

### ✅ 1. حركات المخزون
- ✅ **جميع حركات المخزون** (inventory_transactions) - **شامل لجميع المستودعات والفروع**
- ✅ جميع الحركات بغض النظر عن المستودع أو الفرع أو مركز التكلفة

### ✅ 2. مخزون المنتجات في المستودعات
- ✅ **product_inventory** - المخزون على مستوى المنتج والمستودع
- ✅ جميع السجلات المرتبطة بمنتجات شركة "تست"

### ✅ 3. مخزون المستودعات
- ✅ **warehouse_stock** - المخزون على مستوى المستودع
- ✅ جميع السجلات المرتبطة بمستودعات شركة "تست"

### ✅ 4. حركات المستودعات
- ✅ **warehouse_transactions** - حركات المستودعات
- ✅ جميع الحركات المرتبطة بمستودعات شركة "تست"

### ✅ 5. إهلاكات المخزون
- ✅ **inventory_write_offs** - إهلاكات المخزون
- ✅ **inventory_write_off_items** - عناصر الإهلاك

### ✅ 6. إعادة تعيين المخزون
- ✅ **إعادة تعيين quantity_on_hand = 0** لجميع المنتجات
- ✅ يضمن عدم وجود مخزون سالب أو موجب

---

## 🚀 خطوات التنفيذ

### الطريقة 1: استخدام السكريبت الشامل المحدث

1. **افتح Supabase SQL Editor**
   ```
   Supabase Dashboard → SQL Editor → New Query
   ```

2. **انسخ السكريبت المحدث**
   ```
   افتح: scripts/cleanup-test-company-complete.sql
   انسخ المحتوى بالكامل
   ```

3. **الصق ونفذ**
   ```
   الصق السكريبت في SQL Editor
   اضغط "Run" أو "Execute"
   ```

### الطريقة 2: استخدام السكريبت المخصص للمخزون

إذا كنت تريد تنظيف المخزون فقط (بدون الفواتير):

1. **افتح Supabase SQL Editor**
2. **انسخ السكريبت**
   ```
   افتح: scripts/cleanup-test-company-warehouse-inventory.sql
   انسخ المحتوى بالكامل
   ```
3. **الصق ونفذ**

---

## 📊 التحقق من النتيجة

بعد التنفيذ، يجب أن تكون جميع الأرقام = 0:

| الفئة | العدد المتبقي |
|-------|---------------|
| Inventory Transactions | **0** |
| Products with Stock > 0 | **0** |
| **Products with Stock < 0** | **0** ⭐ |
| **Product Inventory (warehouse level)** | **0** ⭐ |
| **Warehouse Stock** | **0** ⭐ |
| Inventory Write-offs | **0** |

---

## 🔍 التحقق الإضافي

### 1. التحقق من المخزون السالب
```sql
SELECT 
  p.name,
  p.sku,
  p.quantity_on_hand,
  w.name as warehouse_name
FROM products p
LEFT JOIN warehouses w ON w.company_id = p.company_id
WHERE p.company_id = (SELECT id FROM companies WHERE name = 'تست' LIMIT 1)
  AND p.quantity_on_hand < 0;
```

### 2. التحقق من مخزون المستودعات
```sql
SELECT 
  w.name as warehouse_name,
  p.name as product_name,
  pi.quantity_on_hand
FROM warehouses w
CROSS JOIN products p
LEFT JOIN product_inventory pi ON pi.warehouse_id = w.id AND pi.product_id = p.id
WHERE w.company_id = (SELECT id FROM companies WHERE name = 'تست' LIMIT 1)
  AND p.company_id = (SELECT id FROM companies WHERE name = 'تست' LIMIT 1)
  AND (pi.quantity_on_hand IS NOT NULL AND pi.quantity_on_hand != 0);
```

### 3. التحقق من حركات المخزون
```sql
SELECT 
  w.name as warehouse_name,
  b.name as branch_name,
  p.name as product_name,
  it.quantity_change,
  it.transaction_type
FROM inventory_transactions it
LEFT JOIN warehouses w ON w.id = it.warehouse_id
LEFT JOIN branches b ON b.id = it.branch_id
LEFT JOIN products p ON p.id = it.product_id
WHERE it.company_id = (SELECT id FROM companies WHERE name = 'تست' LIMIT 1);
```

---

## ✅ Checklist بعد التنفيذ

- [ ] ✅ تم تنفيذ السكريبت بنجاح
- [ ] ✅ جميع الأرقام في التحقق = 0
- [ ] ✅ **المخزون = 0 لجميع المنتجات** (لا يوجد موجب أو سالب)
- [ ] ✅ **لا توجد حركات مخزون متبقية**
- [ ] ✅ **لا توجد بيانات مخزون في المستودعات**
- [ ] ✅ **لا توجد بيانات مخزون في الفروع**
- [ ] ✅ **لا توجد بيانات مخزون في مراكز التكلفة**
- [ ] ✅ جاهز للاختبار اليدوي

---

## 📁 الملفات

- **السكريبت الشامل المحدث:** `scripts/cleanup-test-company-complete.sql`
- **السكريبت المخصص للمخزون:** `scripts/cleanup-test-company-warehouse-inventory.sql`
- **التعليمات:** `CLEANUP_WAREHOUSE_INVENTORY_INSTRUCTIONS.md` (هذا الملف)

---

## 🏁 النتيجة المتوقعة

بعد التنفيذ الناجح:
- ✅ **جميع بيانات المخزون محذوفة** على مستوى المستودعات
- ✅ **جميع بيانات المخزون محذوفة** على مستوى الفروع
- ✅ **جميع بيانات المخزون محذوفة** على مستوى مراكز التكلفة
- ✅ **المخزون = 0 لجميع المنتجات** (لا يوجد موجب أو سالب)
- ✅ **لا توجد حركات مخزون متبقية**
- ✅ جاهزة للاختبار اليدوي من الصفر

---

**آخر تحديث:** 2026-01-05  
**الحالة:** ✅ **جاهز للتنفيذ**

