# تشخيص مشكلة عدم ظهور الفواتير للمحاسب

## الحالة الحالية
- ✅ أوامر البيع تظهر للمحاسب (60 أمر)
- ❌ الفواتير لا تظهر للمحاسب (0 فاتورة)

## التغييرات المطبقة
1. ✅ تحديث API endpoint للفواتير لجلب بيانات العملاء
2. ✅ إضافة console.log لتتبع المشكلة
3. ✅ تغيير الترتيب من created_at إلى invoice_date

## خطوات التشخيص التالية

### 1. فحص Console Logs
افتح المتصفح وانتقل إلى صفحة الفواتير كمحاسب، ثم افتح Developer Console (F12) وابحث عن:
```
🔍 Invoices API - User Context
📊 Invoices API - Query Result
```

### 2. تشغيل SQL للفحص
```bash
psql <connection_string> -f check-invoices-data.sql
```

### 3. الأسباب المحتملة

#### أ) branch_id غير متطابق
- المحاسب: branch_id = X
- الفواتير: branch_id = Y أو NULL

**الحل**: تشغيل update-governance-clean.sql

#### ب) مشكلة في API endpoint
- الفلتر لا يعمل بشكل صحيح
- accessLevel خاطئ

**الحل**: فحص console.log

#### ج) مشكلة في Frontend
- API يرجع البيانات لكن Frontend لا يعرضها
- خطأ في معالجة response

**الحل**: فحص Network tab في Developer Tools

## الحل المتوقع

إذا كانت المشكلة في branch_id:
```sql
-- تحديث branch_id للفواتير من أوامر البيع
UPDATE invoices i
SET branch_id = so.branch_id
FROM sales_orders so
WHERE i.sales_order_id = so.id
  AND (i.branch_id IS NULL OR i.branch_id != so.branch_id);
```

## التحقق من الحل
بعد تطبيق الإصلاح:
1. تسجيل الدخول كمحاسب
2. الانتقال إلى صفحة الفواتير
3. يجب أن تظهر 61 فاتورة (نفس عدد أوامر البيع تقريباً)

## ملاحظات
- أوامر البيع تعمل بشكل صحيح = API endpoint يعمل
- المشكلة على الأرجح في بيانات الفواتير نفسها (branch_id)
