# 🔐 التدفقات النقدية (Cash Flow Statement) - التوثيق المعماري

**تاريخ الاعتماد:** 2026-01-25  
**الحالة:** ✅ معتمد نهائيًا  
**المستوى:** Architectural Standards

---

## 📋 ملخص

قائمة التدفقات النقدية تصنف التدفقات النقدية إلى: تشغيلية، استثمارية، تمويلية.

**يجب أن يتطابق النقد مع الميزانية العمومية (أرصدة الحسابات البنكية).**

---

## 🔹 مصدر البيانات

### المصدر الوحيد المسموح

```
journal_entries → journal_entry_lines (cash/bank accounts) → cash_flow
```

**يُمنع منعًا باتًا:**
- ❌ قراءة من `payments` مباشرة
- ❌ قراءة من `invoices` مباشرة
- ❌ أي مصدر بيانات آخر

### الجداول المستخدمة

1. **`chart_of_accounts`**
   - فلترة: `sub_type IN ('cash', 'bank')`
   - الحقول: `id`, `sub_type`

2. **`journal_entries`**
   - فلترة: `company_id`, `status = 'posted'`, `deleted_at IS NULL`
   - الفترة: `entry_date BETWEEN from_date AND to_date`

3. **`journal_entry_lines`**
   - الحقول: `account_id`, `debit_amount`, `credit_amount`
   - الربط: `account_id IN (cash_account_ids)`

---

## 🔹 المعادلات المستخدمة

### حساب التدفق النقدي

```typescript
// التدفق النقدي = debit - credit
// موجب = تدفق داخل (زيادة في النقد)
// سالب = تدفق خارج (نقص في النقد)
const cashFlow = debit_amount - credit_amount
```

### تصنيف القيود

```typescript
// Operating: invoice, bill, payment, expense, salary
// Investing: asset_purchase, asset_sale, investment, depreciation
// Financing: loan, capital, dividend, profit_distribution
const category = classify(reference_type)
```

---

## 🔹 العلاقات مع التقارير الأخرى

### التوافق مع الميزانية العمومية

**يجب أن يتحقق دائمًا:**

```
النقد في التدفقات النقدية = أرصدة الحسابات البنكية في الميزانية
```

**التحقق:**
- يتم حساب النقد من نفس المصدر (`journal_entries`)
- أي اختلاف = خطأ نظام

---

## 🔹 القيود المعتمدة

### الفلترة

1. **الشركة:** `company_id` (إلزامي)
2. **الفترة:** `entry_date BETWEEN from_date AND to_date`
3. **الحالة:** `status = 'posted'` و `deleted_at IS NULL`
4. **الحسابات:** فقط `sub_type IN ('cash', 'bank')`

### تصنيف الأنشطة

- **Operating:** الفواتير، المدفوعات، المصروفات، الرواتب
- **Investing:** شراء/بيع الأصول، الاستثمارات، الإهلاك
- **Financing:** القروض، رأس المال، الأرباح الموزعة

---

## 🔹 حالات الخطأ

### 1. عدم تطابق مع الميزانية

**الحالة:** النقد في التدفقات النقدية ≠ أرصدة الحسابات البنكية في الميزانية

**السبب المحتمل:**
- قيود غير متوازنة
- قيود محذوفة
- أخطاء في الحساب

**الإجراء:**
- عرض تحذير واضح
- تسجيل الخطأ في logs
- طلب مراجعة القيود

---

## 📁 الملفات

### API
- **الملف:** `app/api/cash-flow/route.ts`
- **الوظيفة:** حساب التدفقات النقدية من `journal_entries`

### Frontend
- **الملف:** `app/reports/cash-flow/page.tsx`
- **الوظيفة:** عرض قائمة التدفقات النقدية

---

## 🔹 التحسينات المستقبلية

### Realtime Updates

**المطلوب:**
- الاشتراك في تحديثات `journal_entries`
- تحديث التقرير لحظيًا عند إدخال/تعديل قيد
- إعادة التحقق من التوافق مع الميزانية

### التحقق التلقائي

**المطلوب:**
- التحقق من التوافق مع الميزانية تلقائيًا
- عرض تحذير عند أي اختلاف

---

## ✅ معايير الجودة

### الصحة المحاسبية
- ✅ جميع الأرقام قابلة للتتبع إلى `journal_entries`
- ✅ التوافق مع الميزانية محقق
- ✅ لا توجد قيم ثابتة

### الاستقرار التقني
- ✅ لا تعتمد على تخزين مؤقت
- ✅ حساب ديناميكي في كل مرة
- ✅ معالجة أخطاء شاملة

### المطابقة المعيارية
- ✅ مطابق لـ Odoo
- ✅ مطابق لـ Zoho Books
- ✅ مطابق لـ SAP
- ✅ مطابق للمعايير المحاسبية الدولية

---

## 📝 سجل التغييرات

| التاريخ | التغيير | المعتمد |
|---------|---------|---------|
| 2026-01-25 | اعتماد نهائي للمنطق | ✅ |
| 2026-01-25 | إضافة تعليقات توضيحية | ✅ |

---

**تم الاعتماد النهائي ✅**
