# 🔐 القواعد المعمارية النهائية للتقارير المحاسبية

**تاريخ الاعتماد:** 2026-01-25  
**الحالة:** ✅ معتمد نهائيًا - إلزامي على مستوى المشروع  
**المستوى:** Architectural Standards - لا يتم كسرها

---

## 📋 ملخص القواعد

هذه القواعد المعمارية إلزامية لجميع التقارير المحاسبية في النظام. أي تقرير محاسبي يجب أن يلتزم بهذه القواعد بدون استثناء.

**الهدف:** ضمان صحة محاسبية كاملة، توافق بين التقارير، واستقرار تقني للنظام.

---

## 🔹 القاعدة 1: الفصل الصارم بين التقارير المحاسبية والتشغيلية

### التقارير المحاسبية الرسمية

**التقارير التالية تعتبر محاسبية رسمية:**
- ✅ الميزانية العمومية (Balance Sheet)
- ✅ قائمة الدخل (Income Statement)
- ✅ ميزان المراجعة (Trial Balance)
- ✅ التدفقات النقدية (Cash Flow Statement)
- ✅ الأستاذ العام (General Ledger)
- ✅ كشف الحساب (Account Statement)

### المصدر الوحيد المسموح

**جميع التقارير المحاسبية تعتمد حصريًا على:**
```
journal_entries → journal_entry_lines → account_balances → reports
```

**يُمنع منعًا باتًا:**
- ❌ حساب أي رقم محاسبي من `invoices` مباشرة
- ❌ حساب أي رقم محاسبي من `bills` مباشرة
- ❌ حساب أي رقم محاسبي من `stock_movements` مباشرة
- ❌ حساب أي رقم محاسبي من `payments` مباشرة
- ❌ أي مصدر بيانات آخر غير `journal_entries`

### المبدأ الأساسي

```
الفواتير والحركات → تولّد قيود (journal_entries)
التقارير المحاسبية → تقرأ من القيود فقط
```

**مثال صحيح:**
```typescript
// ✅ صحيح: قراءة من journal_entries
const balances = await getAccountBalancesFromJournalEntries(companyId, asOfDate)

// ❌ خطأ: قراءة من invoices مباشرة
const revenue = await sumInvoicesTotal(companyId, period) // ممنوع!
```

### التقارير التشغيلية

**التقارير التشغيلية (غير محاسبية) يمكنها القراءة من:**
- `invoices` - تقارير المبيعات
- `bills` - تقارير المشتريات
- `stock_movements` - تقارير المخزون
- `payments` - تقارير الدفع

**لكن:** لا يمكن استخدامها في التقارير المحاسبية الرسمية.

---

## 🔹 القاعدة 2: قاعدة المصدر الواحد للحقيقة (Single Source of Truth)

### المبدأ

**جميع التقارير المحاسبية تقرأ من نفس المصدر:**

```
journal_entries (القيود)
    ↓
journal_entry_lines (سطور القيود)
    ↓
account_balances (أرصدة الحسابات)
    ↓
    ├─→ Balance Sheet (الميزانية)
    ├─→ Income Statement (قائمة الدخل)
    ├─→ Trial Balance (ميزان المراجعة)
    ├─→ Cash Flow (التدفقات النقدية)
    └─→ General Ledger (الأستاذ العام)
```

### القواعد الإلزامية

1. **لا حساب مكرر:**
   - لا يُسمح بحساب نفس الرقم بطريقتين مختلفتين
   - كل رقم له مصدر واحد فقط

2. **لا مسار بديل:**
   - لا يُسمح بإنشاء مسار بديل للحصول على نفس البيانات
   - جميع التقارير تستخدم نفس الـ API/Function

3. **الاعتماد على Functions المشتركة:**
   ```typescript
   // ✅ صحيح: استخدام function مشتركة
   import { computeAccountBalances } from '@/lib/ledger'
   const balances = await computeAccountBalances(companyId, asOfDate)
   
   // ❌ خطأ: إعادة حساب في كل تقرير
   const balances = await calculateBalancesManually(companyId, asOfDate) // ممنوع!
   ```

### الملفات المشتركة المطلوبة

- `lib/ledger.ts` - حساب الأرصدة من القيود
- `lib/account-balances.ts` - جلب أرصدة الحسابات
- `app/api/account-balances/route.ts` - API موحد للأرصدة

---

## 🔹 القاعدة 3: قاعدة التوافق بين التقارير

### المعادلات الإلزامية

**يجب أن تتحقق دائمًا هذه المعادلات:**

#### 1. ميزان المراجعة = الميزانية
```
مجموع الأرصدة المدينة في ميزان المراجعة = مجموع الأرصدة الدائنة
مجموع الأصول في الميزانية = مجموع الالتزامات + حقوق الملكية
```

#### 2. قائمة الدخل = الميزانية
```
الربح في قائمة الدخل = الربح المرحل في الميزانية (حقوق الملكية)
```

#### 3. التدفقات النقدية = الميزانية
```
النقد في التدفقات النقدية = أرصدة الحسابات البنكية في الميزانية
```

### التحقق الإلزامي

**كل تقرير محاسبي يجب أن:**

1. **يُحقق من التوافق** مع التقارير الأخرى
2. **يُظهر تحذير خطأ نظام** عند أي اختلاف
3. **يُسجل الاختلاف** في logs للتحليل

### مثال التنفيذ

```typescript
// ✅ صحيح: التحقق من التوافق
const balanceSheet = await computeBalanceSheet(companyId, asOfDate)
const trialBalance = await computeTrialBalance(companyId, asOfDate)

if (Math.abs(balanceSheet.totalAssets - trialBalance.totalDebits) > 0.01) {
  throw new Error('SYSTEM ERROR: Balance Sheet and Trial Balance do not match!')
}
```

### حالات الخطأ

**أي اختلاف = خطأ نظام حرج:**
- ❌ لا يُعرض التقرير إذا كان غير متوافق
- ❌ لا يُسمح بتجاهل الاختلافات
- ✅ يجب إصلاح البيانات أولاً

---

## 🔹 القاعدة 4: قاعدة الفروع ومراكز التكلفة

### الدعم الإلزامي

**جميع التقارير المحاسبية يجب أن تدعم:**

- ✅ `company_id` - الشركة (إلزامي)
- ✅ `branch_id` - الفرع (اختياري)
- ✅ `warehouse_id` - المخزن (اختياري)
- ✅ `cost_center_id` - مركز التكلفة (اختياري)

### المنطق الموحد

**نفس المنطق المستخدم في:**
- القيود (`journal_entries`)
- الفواتير (`invoices`)
- الحركات (`stock_movements`)
- التقارير

**بدون منطق خاص لكل تقرير.**

### مثال التنفيذ

```typescript
// ✅ صحيح: استخدام نفس المنطق
interface ReportFilters {
  company_id: string // إلزامي
  branch_id?: string // اختياري
  warehouse_id?: string // اختياري
  cost_center_id?: string // اختياري
  from_date: string
  to_date: string
}

// جميع التقارير تستخدم نفس Interface
const balanceSheet = await getBalanceSheet(filters)
const incomeStatement = await getIncomeStatement(filters)
const trialBalance = await getTrialBalance(filters)
```

### الفلترة

**الفلترة تتم في:**
- `journal_entries` - حسب `branch_id`, `warehouse_id`, `cost_center_id`
- `journal_entry_lines` - حسب الحساب والفرع
- `account_balances` - حسب الفلتر المحدد

---

## 🔹 القاعدة 5: قاعدة Realtime للتقارير الحساسة

### التقارير الحساسة

**التقارير التالية تعتبر حساسة وتحتاج Realtime:**

- ✅ الميزانية العمومية (Balance Sheet)
- ✅ قائمة الدخل (Income Statement)
- ✅ ميزان المراجعة (Trial Balance)
- ✅ كشف الحساب (Account Statement)

### المتطلبات الإلزامية

**عند إدخال أو تعديل قيد (`journal_entry`):**

1. ✅ **يتم تحديث التقرير لحظيًا** - بدون Refresh
2. ✅ **إعادة التحقق من التوازن** - التحقق من المعادلات
3. ✅ **إظهار تحديث واضح** - للمستخدم

### التنفيذ

**استخدام Realtime Manager:**

```typescript
// ✅ صحيح: الاشتراك في تحديثات journal_entries
import { getRealtimeManager } from '@/lib/realtime-manager'

const manager = getRealtimeManager()
manager.on('journal_entries', (event) => {
  if (event.type === 'INSERT' || event.type === 'UPDATE') {
    // تحديث التقرير فورًا
    refreshReport()
    // التحقق من التوازن
    verifyBalance()
  }
})
```

### التقارير غير الحساسة

**التقارير التشغيلية لا تحتاج Realtime:**
- تقارير المبيعات
- تقارير المشتريات
- تقارير المخزون

---

## 🔹 القاعدة 6: توثيق إلزامي لكل تقرير محاسبي

### الملفات المطلوبة

**لكل تقرير محاسبي أساسي يجب وجود ملف توثيق:**

```
docs/
  ├─ BALANCE_SHEET_FINAL_APPROVAL.md ✅
  ├─ INCOME_STATEMENT_ARCHITECTURE.md (مطلوب)
  ├─ TRIAL_BALANCE_ARCHITECTURE.md (مطلوب)
  ├─ CASH_FLOW_ARCHITECTURE.md (مطلوب)
  └─ GENERAL_LEDGER_ARCHITECTURE.md (مطلوب)
```

### محتوى التوثيق الإلزامي

**كل ملف توثيق يجب أن يحتوي على:**

1. **مصدر البيانات:**
   - من أين تأتي البيانات
   - الجداول المستخدمة
   - الـ Queries المستخدمة

2. **المعادلات المستخدمة:**
   - كيف يتم حساب كل رقم
   - الصيغ الرياضية
   - القواعد المحاسبية

3. **العلاقات:**
   - كيف يرتبط التقرير بالتقارير الأخرى
   - المعادلات المشتركة
   - التحقق من التوافق

4. **القيود المعتمدة:**
   - ما هي القيود المطبقة
   - الفلترة المستخدمة
   - الفترات الزمنية

5. **حالات الخطأ:**
   - متى يُعتبر التقرير غير صحيح
   - رسائل الخطأ
   - كيفية الإصلاح

### مثال التوثيق

**راجع:** `docs/BALANCE_SHEET_FINAL_APPROVAL.md` كمرجع.

---

## 🔹 القاعدة 7: ترتيب التنفيذ المعتمد

### المرحلة الأولى: التقارير المحاسبية الأساسية

**الترتيب الصحيح للتنفيذ:**

1. ✅ **الميزانية العمومية (Balance Sheet)** - مكتمل ومعتمد
2. 🔄 **قائمة الدخل (Income Statement)** - التالي
3. ⏳ **ميزان المراجعة (Trial Balance)** - بعد قائمة الدخل
4. ⏳ **الأستاذ العام (General Ledger)** - بعد ميزان المراجعة
5. ⏳ **التدفقات النقدية (Cash Flow)** - بعد الأستاذ العام
6. ⏳ **إغلاق السنة وترحيل الأرباح** - بعد جميع التقارير

### المرحلة الثانية: التقارير التشغيلية

**بعد إكمال المرحلة الأولى:**

- تقارير المبيعات
- تقارير المشتريات
- تقارير المخزون
- تقارير الضرائب

### قاعدة الإكمال

**كل تقرير يجب أن:**

1. ✅ يلتزم بجميع القواعد المعمارية
2. ✅ يكون موثقًا بشكل كامل
3. ✅ يمر بمراجعة محاسبية
4. ✅ يُختبر بشكل شامل
5. ✅ يُعتمد نهائيًا قبل الانتقال للتالي

---

## ✅ ضمانات الجودة

### الصحة المحاسبية

- ✅ جميع الأرقام قابلة للتتبع
- ✅ التوافق بين التقارير محقق
- ✅ المعادلات المحاسبية صحيحة

### الاستقرار التقني

- ✅ لا تعتمد على تخزين مؤقت
- ✅ حساب ديناميكي في كل مرة
- ✅ Realtime للتقارير الحساسة

### قابلية التوسع

- ✅ يعمل مع أي عدد من القيود
- ✅ يعمل مع أي هيكل حسابات
- ✅ يعمل مع أي فترة زمنية
- ✅ يدعم الفروع ومراكز التكلفة

### المطابقة المعيارية

- ✅ مطابق لـ Odoo
- ✅ مطابق لـ Zoho Books
- ✅ مطابق لـ SAP
- ✅ مطابق للمعايير المحاسبية الدولية

---

## ⚠️ تحذيرات مهمة

### عند إضافة تقرير جديد

1. **مراجعة محاسبية إلزامية** قبل أي تنفيذ
2. **الالتزام بجميع القواعد** بدون استثناء
3. **التوثيق الكامل** قبل الاعتماد
4. **التحقق من التوافق** مع التقارير الأخرى

### عند تعديل تقرير موجود

1. **مراجعة التأثير** على التقارير الأخرى
2. **التحقق من التوافق** بعد التعديل
3. **تحديث التوثيق** بشكل فوري
4. **اختبار شامل** قبل النشر

---

## 📝 سجل التغييرات

| التاريخ | التغيير | المعتمد |
|---------|---------|---------|
| 2026-01-25 | اعتماد القواعد المعمارية النهائية | ✅ |

---

## 🔒 الخلاصة

**هذه القواعد المعمارية إلزامية على مستوى المشروع.**

أي تقرير محاسبي يجب أن:
- ✅ يلتزم بجميع القواعد السبع
- ✅ يكون موثقًا بشكل كامل
- ✅ يمر بمراجعة محاسبية
- ✅ يُختبر بشكل شامل

**بهذه القواعد نضمن:**
- ✔ النظام محاسبيًا صحيح
- ✔ التقارير مترابطة
- ✔ لا تضارب أرقام
- ✔ النظام قابل للتوسع بدون إعادة بناء

---

**تم الاعتماد النهائي ✅**
