# 🔒 نظام صلاحيات نقل المخزون (Inventory Transfers Permissions)

## 📋 نظرة عامة

تم تطبيق نظام صلاحيات محكم على صفحات نقل المخزون لضمان عزل كامل بين الفروع والمخازن، مع منع تداخل الصلاحيات.

---

## 🎯 القواعد الأساسية

### ✅ **Owner / Admin**
- ✅ رؤية **جميع** طلبات النقل في جميع المخازن
- ✅ إنشاء طلبات نقل جديدة
- ✅ بدء النقل (خصم من المخزن المصدر)
- ❌ **لا يمكنهم** اعتماد الاستلام (فقط مسؤول المخزن الوجهة)
- ✅ إلغاء طلبات النقل (إذا كانوا هم من أنشأوها)
- ✅ **حذف طلب النقل** (في حالة pending فقط)

### ✅ **Manager (مدير الفرع)**
- ✅ رؤية **فقط** طلبات النقل الخاصة **بفرعه** (المصدر أو الوجهة)
- ❌ **لا يرى** طلبات النقل الخاصة بفروع أخرى
- ✅ إنشاء طلبات نقل جديدة
- ✅ بدء النقل (خصم من المخزن المصدر)
- ❌ **لا يمكنه** اعتماد الاستلام (فقط مسؤول المخزن الوجهة)
- ✅ إلغاء طلبات النقل (إذا كان هو من أنشأها)
- ✅ **حذف طلب النقل** (في حالة pending فقط)

### ✅ **Store Manager (مسؤول المخزن)**
- ✅ رؤية **فقط** الطلبات الموجهة **لمخزنه في فرعه** (`destination_warehouse_id` + `destination_branch_id`)
- ❌ **لا يرى** الطلبات الموجهة لمخازن أخرى أو فروع أخرى
- ❌ **لا يمكنه** إنشاء طلبات نقل جديدة
- ❌ **لا يمكنه** بدء النقل
- ✅ **يمكنه فقط** اعتماد الاستلام للطلبات الموجهة لمخزنه في فرعه
- ❌ **لا يمكنه تعديل الكمية المستلمة** - يستلم الكمية المرسلة كما هي

### ❌ **Staff (موظف)**
- ❌ **لا يمكنه** الوصول لصفحات نقل المخزون

---

## 📄 الصفحات والصلاحيات

### 1️⃣ **صفحة قائمة النقل** (`/inventory-transfers`)

#### الفلترة التلقائية حسب الفرع:
```typescript
// Owner/Admin: يرون جميع الطلبات
if (["owner", "admin"].includes(role)) {
  // لا فلترة
}

// Manager: يرى فقط طلبات النقل الخاصة بفرعه
if (role === "manager" && userBranchId) {
  query.or(`source_branch_id.eq.${userBranchId},destination_branch_id.eq.${userBranchId}`)
}

// Store Manager: يرى فقط الطلبات الموجهة لمخزنه في فرعه
if (role === "store_manager" && userWarehouseId && userBranchId) {
  query.eq("destination_warehouse_id", userWarehouseId)
  query.eq("destination_branch_id", userBranchId)
}
```

#### زر "إنشاء طلب نقل":
- ✅ يظهر لـ: `Owner`, `Admin`, `Manager`
- ❌ لا يظهر لـ: `Store Manager`, `Staff`

---

### 2️⃣ **صفحة تفاصيل النقل** (`/inventory-transfers/[id]`)

#### التحقق من الصلاحيات حسب الفرع:
```typescript
// Store Manager: يمكنه فقط رؤية الطلبات الموجهة لمخزنه في فرعه
if (role === "store_manager" && userWarehouseId && userBranchId) {
  if (transfer.destination_warehouse_id !== userWarehouseId ||
      transfer.destination_branch_id !== userBranchId) {
    // رفض الوصول وإعادة التوجيه
    router.push("/inventory-transfers")
  }
}

// Manager: يمكنه فقط رؤية الطلبات الخاصة بفرعه
if (role === "manager" && userBranchId) {
  if (transfer.source_branch_id !== userBranchId &&
      transfer.destination_branch_id !== userBranchId) {
    // رفض الوصول وإعادة التوجيه
    router.push("/inventory-transfers")
  }
}
```

#### الأزرار والإجراءات:

| الإجراء | Owner/Admin | Manager | Store Manager |
|---------|-------------|---------|---------------|
| **بدء النقل** | ✅ | ✅ | ❌ |
| **اعتماد الاستلام** | ❌ | ❌ | ✅ (مخزنه الوجهة فقط) |
| **تعديل الكمية المستلمة** | ❌ | ❌ | ❌ |
| **إلغاء النقل** | ✅ (إذا أنشأه) | ✅ (إذا أنشأه) | ❌ |

---

### 3️⃣ **صفحة إنشاء طلب نقل** (`/inventory-transfers/new`)

#### التحقق من الصلاحيات:
```typescript
// فقط Owner/Admin/Manager يمكنهم إنشاء طلبات نقل
if (!["owner", "admin", "manager"].includes(role)) {
  toast({ title: 'غير مصرح' })
  router.push("/inventory-transfers")
}
```

---

## 🔐 أمثلة عملية

### مثال 1: مسؤول مخزن مدينة نصر
- **الدور:** `store_manager`
- **الفرع:** `فرع مدينة نصر`
- **المخزن:** `مخزن مدينة نصر`

**ما يراه:**
- ✅ طلب نقل من "المخزن الرئيسي (فرع آخر)" → "مخزن مدينة نصر (فرع مدينة نصر)"
- ❌ طلب نقل من "المخزن الرئيسي" → "مخزن مصر الجديدة (فرع آخر)"
- ❌ طلب نقل من "مخزن مدينة نصر" → "مخزن آخر" (لأنه ليس الوجهة)

**ما يمكنه فعله:**
- ✅ اعتماد استلام الطلبات الموجهة لمخزنه في فرعه (بالكمية المرسلة فقط، بدون تعديل)
- ❌ تعديل الكمية المستلمة
- ❌ إنشاء طلبات نقل جديدة
- ❌ بدء النقل

---

### مثال 2: مدير فرع مدينة نصر
- **الدور:** `manager`
- **الفرع:** `فرع مدينة نصر`

**ما يراه:**
- ✅ طلبات النقل من "مخزن مدينة نصر" → "مخزن آخر"
- ✅ طلبات النقل من "مخزن آخر" → "مخزن مدينة نصر"
- ❌ طلبات النقل بين فروع أخرى (مثل: "فرع القاهرة" → "فرع الإسكندرية")

**ما يمكنه فعله:**
- ✅ إنشاء طلبات نقل جديدة (من/إلى فرعه)
- ✅ بدء النقل (خصم من المخزن المصدر في فرعه)
- ✅ إلغاء النقل (إذا كان هو من أنشأه)
- ❌ اعتماد الاستلام (يجب أن يقوم به مسؤول المخزن الوجهة فقط)

---

## 🔄 تحديث الفرع للمستخدم (Critical Rule)

### القاعدة الذهبية:
عند تغيير فرع المستخدم في صفحة المستخدمين:
1. ✅ **يتم تحديث صلاحيات العرض فوراً**
2. ✅ **لا يرى المستخدم بعد التغيير إلا طلبات نقل المخزون الخاصة بالفرع الجديد فقط**
3. ❌ **يُمنع الاحتفاظ بأي Cache أو Session قد يسمح برؤية طلبات تخص الفرع السابق**

### التطبيق:
```typescript
// في كل صفحة، يتم جلب branch_id من قاعدة البيانات مباشرة
const { data: member } = await supabase
  .from("company_members")
  .select("role, branch_id, warehouse_id")
  .eq("company_id", companyId)
  .eq("user_id", user.id)
  .single()

// ✅ استخدام branch_id الحالي مباشرة (بدون cache)
const userBranchId = member?.branch_id || null

// ✅ تطبيق الفلترة فوراً
if (role === "manager" && userBranchId) {
  query.or(`source_branch_id.eq.${userBranchId},destination_branch_id.eq.${userBranchId}`)
}
```

### مثال عملي:
**السيناريو:**
- مستخدم: `أحمد`
- الدور: `manager`
- الفرع الحالي: `فرع مدينة نصر`
- يرى: 50 طلب نقل خاص بفرع مدينة نصر

**بعد تغيير الفرع إلى `فرع القاهرة`:**
- ✅ يرى فوراً: 30 طلب نقل خاص بفرع القاهرة
- ❌ لا يرى: أي طلب من فرع مدينة نصر السابق
- ✅ الصلاحيات تتحدث تلقائياً بدون إعادة تسجيل دخول

---

## 🛡️ الحماية من التلاعب

### 1️⃣ **فلترة على مستوى قاعدة البيانات**
- جميع الاستعلامات تطبق فلترة `destination_warehouse_id` لمسؤولي المخازن
- لا يمكن الوصول للبيانات حتى لو تم التلاعب بالـ URL

### 2️⃣ **التحقق من الصلاحيات في كل صفحة**
- كل صفحة تتحقق من صلاحيات المستخدم قبل عرض البيانات
- إعادة توجيه تلقائية في حالة عدم وجود صلاحية

### 3️⃣ **عزل كامل بين المخازن**
- مسؤول مخزن A لا يمكنه رؤية أو التعامل مع طلبات مخزن B
- Owner/Admin فقط لديهم رؤية شاملة

---

## 📊 ملخص الصلاحيات

| الدور | رؤية الطلبات | إنشاء | بدء النقل | اعتماد الاستلام | تعديل الكمية | إلغاء |
|-------|--------------|-------|-----------|-----------------|--------------|-------|
| **Owner** | الكل | ✅ | ✅ | ❌ | ❌ | ✅ (إذا أنشأه) |
| **Admin** | الكل | ✅ | ✅ | ❌ | ❌ | ✅ (إذا أنشأه) |
| **Manager** | الكل | ✅ | ✅ | ❌ | ❌ | ✅ (إذا أنشأه) |
| **Store Manager** | مخزنه الوجهة فقط | ❌ | ❌ | ✅ (مخزنه الوجهة) | ❌ | ❌ |
| **Staff** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

---

## ✅ التطبيق

تم تطبيق هذا النظام في:
- ✅ `app/inventory-transfers/page.tsx` (قائمة النقل)
- ✅ `app/inventory-transfers/[id]/page.tsx` (تفاصيل النقل)
- ✅ `app/inventory-transfers/new/page.tsx` (إنشاء طلب نقل)

---

## 🎯 الفصل الواضح بين الأدوار

### 📤 **الإرسال (Owner/Admin/Manager)**
- ✅ يمكنهم إنشاء طلبات النقل
- ✅ يمكنهم بدء النقل (خصم من المخزن المصدر)
- ❌ **لا يمكنهم** اعتماد الاستلام

### 📥 **الاستلام (Store Manager فقط)**
- ❌ **لا يمكنهم** إنشاء طلبات النقل
- ❌ **لا يمكنهم** بدء النقل
- ✅ يمكنهم **فقط** اعتماد الاستلام في مخزنهم الوجهة

### 🔑 **القاعدة الذهبية:**
> **من يرسل لا يستلم، ومن يستلم لا يرسل**

---

## 🚫 قواعد إلغاء طلبات النقل

### ✅ **متى يُسمح بالإلغاء؟**

1. **الحالة المطلوبة:** فقط عندما يكون الطلب في حالة **"In Transit / قيد النقل"**
2. **المستخدم المصرح:** فقط المستخدم الذي **أنشأ الطلب** (`created_by`)

### ❌ **متى يُمنع الإلغاء؟**

1. ❌ بعد اكتمال الاستلام (`status = 'received'`)
2. ❌ في حالة الانتظار (`status = 'pending'`)
3. ❌ من قبل مستخدم آخر غير الذي أنشأ الطلب

### 🔄 **ماذا يحدث عند الإلغاء؟**

1. ✅ **إرجاع الكميات بالكامل** إلى المخزن المصدر
2. ✅ **تسجيل حركة مخزون** من نوع `transfer_cancelled`
3. ✅ **تحديث حالة الطلب** إلى `cancelled`
4. ✅ **حفظ سبب الإلغاء** في `rejection_reason`

### 🛡️ **رسالة الخطأ للمستخدم غير المصرح:**

**بالعربية:**
```
لا يمكن إلغاء طلب نقل المخزون إلا من قبل المستخدم الذي قام بإنشائه: [اسم المستخدم]
```

**بالإنجليزية:**
```
Only the user who created this transfer can cancel it: [User Name]
```

### 📊 **مثال عملي:**

**السيناريو:**
- طلب نقل رقم `TRF-001`
- أنشأه: `أحمد محمد`
- الحالة: `in_transit`

**النتيجة:**
- ✅ `أحمد محمد` يمكنه إلغاء الطلب
- ❌ `محمد علي` (مدير) لا يمكنه إلغاء الطلب
- ❌ `Owner` لا يمكنه إلغاء الطلب (إلا إذا كان هو من أنشأه)

### 🎯 **الهدف:**

1. ✅ **الحفاظ على سلامة أرصدة المخزون**
2. ✅ **منع التلاعب أو الإلغاء غير المصرح به**
3. ✅ **ضمان تتبع واضح للمسؤوليات** (Audit Trail)

---

## 🗑️ حذف طلبات النقل (Delete Transfer)

### 📋 **القواعد:**

1. ✅ **من يمكنه الحذف:**
   - Owner
   - Admin
   - Manager

2. ✅ **متى يمكن الحذف:**
   - فقط عندما يكون الطلب في حالة **pending** (قيد الانتظار)
   - ❌ لا يمكن حذف طلب في حالة `in_transit` أو `received`

3. ✅ **ماذا يحدث عند الحذف:**
   - يتم حذف طلب النقل نهائياً من قاعدة البيانات
   - يتم حذف جميع بنود النقل المرتبطة
   - لا توجد حركات مخزون لإلغائها (لأن الطلب لم يبدأ بعد)

### 📊 **مثال عملي:**

**السيناريو 1: حذف ناجح ✅**
- طلب نقل رقم `TRF-001`
- الحالة: `pending`
- المستخدم: `مدير` أو `Owner`
- النتيجة: ✅ يتم حذف الطلب بنجاح

**السيناريو 2: حذف مرفوض ❌**
- طلب نقل رقم `TRF-002`
- الحالة: `in_transit`
- المستخدم: `Owner`
- النتيجة: ❌ لا يمكن الحذف (يجب إلغاء النقل أولاً)

**السيناريو 3: حذف مرفوض ❌**
- طلب نقل رقم `TRF-003`
- الحالة: `pending`
- المستخدم: `Store Manager`
- النتيجة: ❌ لا يمكن الحذف (ليس لديه صلاحية)

### 🎯 **الهدف:**

1. ✅ **السماح بحذف الطلبات الخاطئة قبل بدء النقل**
2. ✅ **منع حذف الطلبات التي بدأت أو اكتملت** (للحفاظ على سلامة المخزون)
3. ✅ **تقييد الحذف على المستخدمين المصرح لهم فقط**

---

**آخر تحديث:** 2026-01-03

