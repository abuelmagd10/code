# كيفية تشغيل سكريبتات المراجعة اليدوية

## ⚠️ ملاحظة مهمة

إذا واجهت خطأ `syntax error at or near "#"`، فهذا يعني أنك تحاول تشغيل ملف PowerShell (`.ps1`) بدلاً من ملف SQL (`.sql`).

## ✅ الطرق الصحيحة لتشغيل السكريبتات

### الطريقة 1: تشغيل السكريبتات بشكل منفصل (موصى به)

يمكنك تشغيل كل سكريبت بشكل منفصل:

```sql
-- خطوة 1: عرض جميع المدفوعات
\i scripts/review_payments_step1.sql

-- خطوة 2: مقارنة المدفوعات مع فواتير الشراء
\i scripts/review_payments_step2.sql

-- خطوة 3: عرض جميع إشعارات الدائن
\i scripts/review_payments_step3.sql

-- خطوة 4: مقارنة إشعارات الدائن مع فواتير الشراء
\i scripts/review_payments_step4.sql

-- خطوة 5: ملخص شامل
\i scripts/review_payments_step5.sql
```

### الطريقة 2: استخدام psql من سطر الأوامر

```bash
# تشغيل سكريبت SQL واحد
psql -h localhost -U postgres -d your_database -f scripts/review_payments_step1.sql

# أو تشغيل جميع السكريبتات
psql -h localhost -U postgres -d your_database -f scripts/review_payments_step1.sql
psql -h localhost -U postgres -d your_database -f scripts/review_payments_step2.sql
psql -h localhost -U postgres -d your_database -f scripts/review_payments_step3.sql
psql -h localhost -U postgres -d your_database -f scripts/review_payments_step4.sql
psql -h localhost -U postgres -d your_database -f scripts/review_payments_step5.sql
```

### الطريقة 3: استخدام PowerShell Script (للمستخدمين المتقدمين)

```powershell
# تشغيل سكريبت PowerShell
.\scripts\review_payments_and_vendor_credits.ps1
```

**ملاحظة**: سكريبت PowerShell يتطلب:
- تثبيت `psql` (PostgreSQL client)
- إدخال معلومات الاتصال بقاعدة البيانات

### الطريقة 4: نسخ ولصق الاستعلامات مباشرة

يمكنك فتح ملف SQL ونسخ محتوى الاستعلام ولصقه مباشرة في أداة SQL الخاصة بك (مثل pgAdmin، DBeaver، أو Supabase SQL Editor).

## 📋 قائمة السكريبتات المتاحة

### السكريبتات الأساسية (خطوة بخطوة):
1. `scripts/review_payments_step1.sql` - عرض جميع المدفوعات
2. `scripts/review_payments_step2.sql` - مقارنة المدفوعات مع فواتير الشراء
3. `scripts/review_payments_step3.sql` - عرض جميع إشعارات الدائن
4. `scripts/review_payments_step4.sql` - مقارنة إشعارات الدائن مع فواتير الشراء
5. `scripts/review_payments_step5.sql` - ملخص شامل

### السكريبتات الشاملة:
- `scripts/review_payments_and_vendor_credits.sql` - جميع الاستعلامات في ملف واحد
- `scripts/review_specific_payments.sql` - مراجعة المدفوعات الكبيرة والمشبوهة

### سكريبتات PowerShell:
- `scripts/review_payments_and_vendor_credits.ps1` - واجهة تفاعلية لتشغيل السكريبتات

## 🔍 حل المشاكل الشائعة

### المشكلة: `syntax error at or near "#"`
**الحل**: تأكد من أنك تشغّل ملف `.sql` وليس ملف `.ps1`

### المشكلة: `relation "payments" does not exist`
**الحل**: تأكد من أنك متصل بقاعدة البيانات الصحيحة

### المشكلة: `permission denied`
**الحل**: تأكد من أن المستخدم لديه صلاحيات القراءة على الجداول المطلوبة

## 📞 الدعم

إذا واجهت أي مشاكل، يرجى:
1. التحقق من أنك تستخدم ملف `.sql` وليس `.ps1`
2. التحقق من اتصالك بقاعدة البيانات الصحيحة
3. التحقق من صلاحيات المستخدم

---

**تاريخ الإنشاء**: 2024-01-XX
